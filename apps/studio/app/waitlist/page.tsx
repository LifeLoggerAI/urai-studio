'use client';import { useState } from 'react';
export default function Page(){const [msg,setMsg]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); const email=String(fd.get('email')||''); const website=String(fd.get('website')||''); if(!email.includes('@')) return setMsg('Enter a valid email.'); const r=await fetch('/api/waitlist',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,website})}); const j=await r.json(); setMsg(j.message||j.error);} return <section><h1>Join Waitlist</h1><form onSubmit={submit} className='card'><input name='email' placeholder='Email' /><input name='website' style={{display:'none'}} tabIndex={-1}/><button>Join</button></form><p>{msg}</p></section>}
