'use client';import { useState } from 'react';
export default function Page(){const [msg,setMsg]=useState(''); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); const email=String(fd.get('email')||''); const message=String(fd.get('message')||''); const website=String(fd.get('website')||''); if(!email.includes('@')||message.length<8) return setMsg('Provide a valid email and message.'); const r=await fetch('/api/contact',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,message,website})}); const j=await r.json(); setMsg(j.message||j.error);} return <section><h1>Contact URAI Labs LLC</h1><form onSubmit={submit} className='card'><input name='email' placeholder='Email'/><textarea name='message' placeholder='Tell us what you want to build.'/><input name='website' style={{display:'none'}} tabIndex={-1}/><button>Send</button></form><p>{msg}</p></section>}
