{
  "name": "urai-studio-functions",
  "description": "Cloud Functions for URAI Studio",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:membership-migration": "node --test scripts/studio-membership-migration.test.mjs",
    "membership:migrate": "node scripts/studio-membership-migration.mjs"
  },
  "dependencies": {"firebase-admin": "^12.0.0", "firebase-functions": "^5.0.0"},
  "devDependencies": {"typescript": "^5.4.5"},
  "private": true,
  "engines": {"node": "20"}
}
