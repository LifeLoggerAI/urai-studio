# Renderer Status

A Studio renderer package has been added at apps/studio/lib/studio-video-renderer.ts.

It builds a deterministic package from the existing Video Factory template.

The remaining runtime task is to connect browser capture and final file composition in a worker environment.
